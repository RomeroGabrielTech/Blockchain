var primo = 100;


for (let y = primo;)
    for (let i = primo; i > 0; i--) {
        let div = (primo / i);
        let resto = (primo % i);

        console.log("division de " + primo + "/" + i + " es " + div + "El resto es " + resto);

        /*    if ((primo % i) == 0) {
                console.log("El número " + i + " es primo");
            }*/
    }